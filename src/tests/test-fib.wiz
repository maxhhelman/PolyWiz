def int fib(int x)
{
  if (x < 2) return 1;
  return fib(x-1) + fib(x-2);
}

def int main()
{
  printint(fib(0));
  printint(fib(1));
  printint(fib(2));
  printint(fib(3));
  printint(fib(4));
  printint(fib(5));
  return 0;
}
