def int gcd(int a, int b) {
  while (a != b)
    if (a > b) a = a - b;
    else b = b - a;
  return a;
}

def int main()
{
  printint(gcd(14,21));
  printint(gcd(8,36));
  printint(gcd(99,121));
  return 0;
}
