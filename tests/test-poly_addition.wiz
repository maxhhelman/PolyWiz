def int main()
{
  poly poly1;
  poly poly2;
  poly poly_sum;

  poly1 = new_poly([1.0, 2.1, 4.0], [2, 1, 0]);
  poly2 = new_poly([1.0, -1.0, 3.2, 0.0], [3, 2, 1, 0]);
  poly_sum = poly1 + poly2;

  printf(el_at_ind(poly_sum, 1));

  return 0;
}
